<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Doggo Guess | <%- name %></title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <h2 class="title">O seu doguinho (ou provavelmente)</h2>
    <div style="background-image: url('<%- img %>')" class="dog-photo"></div>
    <h3><%- name %></h3>
    <p>Idade: <%- age %> anos</p>
    <p>Prováveis nacionalidades: </p>
    <ul>
        <% if(nation.length > 0) { %>
            <% nation.map(country => { %>
                <li><%- country.country_name %> <%- (country.probability*100).toFixed(0) %>% </li>
            <% }) %>
        <% } else { %>
            <p>Não foi possível adivinhar a nacionalidade...</p>
        <% } %>
    </ul>
    <div class="button">
        <a href="/index.html">Testar novamente</a>
    </div>
</body>
</html>